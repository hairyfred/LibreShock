# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Claude Code local settings
.claude/

# Captured BLE traffic (per-device, contains MAC addresses & device-specific data)
captures/

# Pre-rewrite scratch scripts — contain hardcoded personal device MAC.
# Kept locally as historical reference, not for the public repo.
scripts/old/

# Keystores and signing creds — never commit these
*.jks
*.keystore
keystore.properties

# IDE
.vscode/
*.swp

# JVM crash dumps (occasionally appear after a Gradle test executor crash)
hs_err_pid*.log
